# Gradle / build output
.gradle/
build/
app/build/
*/build/

# Local configuration (machine-specific, contains SDK path)
local.properties

# Android Studio / IntelliJ
.idea/
*.iml
.DS_Store
captures/
.externalNativeBuild/
.cxx/

# Logs
*.log

# Signing secrets — NEVER commit these
keystore.properties
keystore/
*.jks
*.keystore

# Distributable APKs live under apk/devel and apk/release and ARE tracked
# (the apk/ folder is not under build/, so it is not ignored above).
